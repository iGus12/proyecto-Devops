version: '3.8'

services:
  back-ventas:
    build: .
    container_name: back-ventas-container
    ports:
      - "8080:8080"  
    environment:
      - DB_ENDPOINT=${DB_ENDPOINT}  
      - DB_PORT=3306
    restart: always